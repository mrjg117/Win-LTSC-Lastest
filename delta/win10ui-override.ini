; 本文件只放「不随配置变的红线」，由 99.Force-W10UI-Ini.ps1 在调用 W10UI.cmd 之前写回 W10UI.ini。
; 优化清单不在这里 —— 写进 config.yml 的 optimize.ini，99 会自动把对应键置 1。
; 不整文件覆盖上游 W10UI.ini，只确保这些键的值到位，免疫上游后续加键。
; 红线：不转 ESD、ResetBase=0（保留组件可卸载性）。
[Options]
wim2esd=0
ResetBase=0
